rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /filmagens/{filmagem_id} {
      allow read, write: if request.auth != null;
    }
    match /filmagens/{filmagem_id}/execucoes/{item_id} {
      allow read, write: if request.auth != null;
    }
  }
}
